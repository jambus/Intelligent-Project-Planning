{
  "name": "ssp-root",
  "version": "1.0.8",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "dev": "npm run dev --prefix extension",
    "build": "npm run build --prefix extension",
    "zip": "npm run zip --prefix extension",
    "crx": "mkdir -p release && npm run build --prefix extension && npx crx3 extension/dist -p privatekey.pem -o release/irp.crx",
    "crx-ssp": "mkdir -p release && npm run build --prefix extension && npx crx3 extension/dist -p chrome_dev_private_key.pem -o release/ssp.crx",
    "publish": "npm run publish --prefix extension"
  }
}