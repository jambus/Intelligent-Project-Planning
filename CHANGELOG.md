# Changelog

## [1.0.7] - 2026-06-05

### 优化与修复 (Improvements & Fixes)
- **排期引擎状态同步与跨边界约束 (State Sync & Boundaries)**：
  - 重构 `applySuggestions` 参数传递，通过显式下传 `isLatePass` 取代读取异步的 `currentStep` 状态。修复了 `cross-team`（跨队借人）模式在最后排期阶段偶尔不生效的问题。
  - 在大模型请求 (`suggestAllocationsForBatch`) 前置了严格的候选池裁剪。系统会根据当前批次项目的 `allowedResourceIds` (Scrum 约束) 过滤闲置人员，AI 只会看到合法范围内的候选人。大幅降低大模型 Token 消耗，彻底消灭 AI 瞎编和幻觉推荐。
  - 修复 `focused`（专注模式）的跨次重排漏洞，结合历史分配记录和跨 Pass 的 Set 控制，防止对同一专注项目重复指派多名人员。
- **大盘一致性与节假日扣减 (Dashboard & Holiday Sync)**：
  - 排期大盘的 `gap` 缺口计算和 `calculateWeeklyMD`（每周折算工时）全面引入当前 `workingDaySet`。彻底解决“已排满 / 部分排满”状态评估，以及周维度投入工时计算因节假日口径不一致导致的 UI 展示错误。
- **项目与团队配置连动联动 (Team Config Cascade)**：
  - 级联删除优化：在「Scrum 管理」解散团队时，系统不仅清理人员归属，还会将对应项目的 `scrumTeamId` 设为空，并强制降级调度模式为 `all-in`（全局统筹），防止项目因找不到废弃团队的人员陷入排期死锁。
  - 在「项目配置」页修改策略或 Scrum 约束条件时，系统会自动清空上一次遗留的 `rejectionReason`（排不上原因），避免大盘状态未更新产生视觉误导。
- **排期大盘深度重构与架构升级 (Dashboard Refactoring & Architecture)**：
  - **核心计算与状态下沉**：引入全局 `DashboardContext` 上下文，统一接管所有 IndexedDB `useLiveQuery` 订阅以及重量级的缺口/容量计算 (`runAuditForUI`)，彻底解决了页面切换带来的状态重置与重复渲染问题，实现了跨页面的 Single Source of Truth。
  - **页面精细化拆分**：将原先臃肿的单一大盘拆解为 4 个职责单一、高内聚的子页面组件：`DashboardOverview` (全局概览与排期控制台)、`TeamCapacity` (团队容量监控)、`ProjectResults` (排期异常及结果展示) 和 `ScheduleDetails` (排期明细大矩阵)，极大提升了代码可维护性和用户浏览体验。
  - **概览面板增强**：在全局概览页面，移除了原有的“最紧张的团队 Top 3”局部视图，直接升级为展示完整的“Scrum 团队容量 (当前选定时段)”看板，让全局负荷水位一目了然。

## [1.0.6] - 2026-06-05

### 新特性 (New Features)
- **Scrum 团队管理 (Scrum Team Management)**：
  - 新增独立的「Scrum 管理」侧边栏菜单，支持 Scrum 团队的创建、名称及描述的编辑、删除等全生命周期管理。
  - 在团队维度下支持跨团队添加或移除开发/测试人员。删除团队时，采用级联保护策略自动解除人员的团队归属，不删除人员数据本身。
  - 「人员管理」大盘新增「所属 Scrum」列，方便全局纵览资源的归属状态。
  - **导入导出全面支持**：CSV 文件批量导入及人员导出均全面兼容并新增了 `Scrum Team` 字段，导入时如果遇到系统中不存在的团队将自动为您无缝创建。
- **Scrum 团队升级为排期硬约束 (Scrum Team as Hard Scheduling Constraints)**：
  - **团队协作模式**：在项目级别引入 `team-first`（本队专属）、`cross-team`（跨队借人）和 `all-in`（全局统筹）三种团队调度模式约束，可在「项目管理」大盘可视化编辑或通过 CSV 导入。
  - **底层硬拦截 (JS Hard Logic)**：排期引擎会根据当前调度轮次动态收放候选池 `allowedResourceIds`：`team-first` 永远禁锢在本队；`cross-team` 仅在最后收割轮次对外借人。系统主动丢弃 AI 所有越界分配建议，彻底消灭大模型跨团队瞎分配的幻觉。
- **全局排期大盘精细化拆分 (Dashboard Sub-menus Restructuring)**：
  - 为了解决单个大盘承载数据过多导致向下浏览不便的问题，我们将「仪表盘」菜单升级为拥有 4 个子视图的层级结构：`全局概览`、`已排项目`、`已排期任务详情`、`待跟进事项`。
  - **局部隔离与全局联动**：无论您切换至哪个子视图，顶部的“系统排班控制台”（选时、继续/智能排班）以及“4大核心数字指标卡”将全局常驻悬浮可见。下方的长列表表格则按需动态渲染，显著提升了滚屏与阅读体验。
- **排期状态细化与归因推断 (Refined Scheduling Status & Heuristics)**：
  - 废弃大盘原有的简单“已排/未排”二元展示，将所有项目（按优先级）拆分为三类：**已排满项目**（开发测试需求全覆盖）、**部分排上项目**（有缺口但已有资源分配）、**排不上项目**（完全无资源分配）。
  - 新增智能启发式归因，系统会在“排不上项目”后自动推测并直观标注原因（如：`开发容量不足`、`测试容量不足`、`Lead 不可用` 或 `时间窗口不足`）。
- **Scrum 团队容量监控 (Scrum Team Capacity Monitoring)**：
  - 全局大盘新增「Scrum 团队容量」可视化区块，自动基于当前选定时间窗口，按团队汇总总容量、已用容量、总剩余及细分的“开发剩余 / 测试剩余”人天。
  - **容量计算精准化**：Dashboard 容量大盘现在与排期引擎共享严格的工作日计算逻辑（`buildWorkingDaySet`），在统计总容量时能够精确扣减个人请假天数，从而解决了大盘容量虚高与真实可排产能不一致的问题。
  - 通过直观的彩色饱和度百分比与进度条，管理层可实时掌握各团队及全局闲散人员的当前负荷水位线。
- **排期大盘便捷时间筛选 (Quick Time Range Filters)**：
  - 排期大盘的头部控制台新增快捷时间区间筛选按钮（如“本月起3个月”、“本季度”、“下季度”），一键即刻应用至时间选择器。
- **Jira 同步时间范围过滤 (Jira Sync Date Range Filtering)**：
  - 在「Jira 管理」同步页面中新增时间范围选择器，默认选中当前月前3个月至当前月末的范围。
  - 同步时自动在底层的 JQL 中追加 `worklogDate` 过滤条件，支持仅同步特定时间段内活跃的工时记录。

## [1.0.5] - 2026-05-31

### 新特性 (New Features)
- **Jira 工时归属精准化 — 基于 Worklog 作者角色 (Role-Based Worklog Attribution)**：
  - **作者级工时拆分**：不再仅依赖 Issue Type 推断开发/测试工时。系统现在逐条解析 worklog 作者身份，与「人员管理」中的团队花名册进行匹配：测试工程师的 worklog 归入 `testLoggedMd`，开发角色归入 `devLoggedMd`，真正精确到"谁记的工时，就按谁的角色归类"。
  - **Jira 账号映射（Jira Aliases）**：当团队成员的中文姓名与 Jira 显示名不一致（英文名、昵称、共享账号）时，可在「人员管理」编辑页中配置 Jira 别名（accountId / 邮箱 / 显示名，多个用逗号分隔）。匹配优先级：accountId/邮箱 精确匹配 > 显示名/姓名 模糊匹配。
  - **未匹配作者主动提示**：同步完成后，若有 worklog 作者无法对应到任何团队成员，系统会**主动弹出琥珀色提示面板**，按工时从多到少列出每位未匹配的 Jira 用户（显示名、身份标识、累计工时）。用户可在下拉框中选择对应成员并点击「绑定」，系统自动将其 Jira 身份写入该成员的别名，配置后重新同步即可精确归属。
  - **CSV 导入/导出支持 Jira Aliases**：人员导出 CSV 新增 `Jira Aliases` 列；导入时自动识别该列（支持 `Jira Aliases` / `Jira ID` / `Jira别名` / `Jira账号` / `Jira映射` 等表头），实现批量配置。

- **多 Epic 状态聚合显示 (Multi-Epic Status Aggregation)**：
  - 当一个项目关键词（尤其通过模糊匹配）同时命中多个物理 Epic 时，Epic 状态不再被最后一个覆盖，而是将所有 **去重后的不同状态** 用 ` / ` 连接显示（如 `进行中 / 已完成`）。Story/Task/Bug 计数继续跨所有命中 Epic 累加。

### 修复与体验 (Fixes & UX)
- **人员编辑弹窗溢出修复 (Resource Modal Overflow Fix)**：
  - 修复了当团队成员技能标签较多时，编辑弹窗高度超出屏幕导致关闭按钮和确认/取消按钮不可见、无法滚动的问题。弹窗现在限制最大高度为 `85vh` 并支持内部滚动，外层遮罩也改为可整体滚动。

### 核心引擎与精准度 (Core Engine & Precision)
- **时区错位修复 (Timezone-Safe Dates)**：
  - 排期全链路日期键不再使用会因 UTC 偏移跨日的 `toISOString().split('T')[0]`，统一改为基于本地时间的 `formatLocalDate()`。彻底解决了东八区下凌晨时段排期、工作日计数与节假日判定可能整体偏移一天的隐患。
- **节假日配置参与排期 (Holiday-Aware Scheduling)**：
  - 每次排期前会先从本地配置（`holidays`/`specialWorkdays`）动态加载用户自定义的节假日与调休日，确保「节假日管理」中的设置真正作用于排期计算，而非仅依赖内置默认值。
- **统一缺口计算与累计取整 (Unified Gap Calculation)**：
  - 抽取共享纯函数 `computeProjectGaps`，让排期引擎 (`runAudit`) 与大盘审计 (`runAuditForUI`) 使用同一套缺口口径。人天累计全程保留浮点精度，仅在最终展示/写入时取整，消除了逐条四舍五入累积出来的偏差。
- **资源请假纳入排期 (Leave-Aware Capacity)**：
  - 生成资源日历时读取每个人的 `unavailableDates`，请假当日产能直接置 0，避免把工作排到员工的休假日。
- **越界人天重算修正 (Out-of-Range MD Fix)**：
  - 将排期终点越界截断的时机提前到实际工作日/人天重算之前，修正了跨越排期窗口上限时人天被高估的问题。

### 健壮性与体验 (Robustness & UX)
- **调度死循环保护**：`calculateEndDate` 改为带迭代上限的有界循环，防止极端节假日配置导致卡死。
- **回滚性能优化**：PASS 2 完整性回滚不再在循环内反复重建共享矩阵，改为按需清理一次，消除 O(n²) 开销。
- **AI 调用超时可配置与解析加固**：AI 请求超时改为可在「系统设置」中按秒自定义，默认放宽至 180s，以适配 DeepSeek 等单批耗时较长的推理型模型（此前固定 60s 硬超时会导致这类模型每次排期都被中断）；仍区分「超时」与「外部主动停止」两类中断，收紧分配占比上限至 100%，并对返回结构（无数组括号 / JSON 解析失败 / `choices` 缺失）做了分级容错。
- **Jira 同步增强**：同步过程新增逐项目进度展示与按 Epic 维度的错误汇总；新增「Epic Link 自定义字段 ID」设置项，支持非默认 `customfield_10014` 的 Jira 实例。
- **数据安全防护**：项目/人员文件导入为「清空并覆盖」操作，导入前若已有数据会弹窗二次确认；删除资源/项目时级联清理其关联的排期记录，杜绝孤儿数据。

### 新特性 (New Features)
- **周级别粒度排期大盘 (Weekly Granularity Dashboard)**：
  - **双层多级表头视图**：将原先粗粒度的“月份”表头升级为“按月统领，按自然周细分”（例如 5 月统领 W18, W19, W20...）的立体双层大盘结构，显著提升排期可见度。
  - **精准折算周投入**：底层时间换算逻辑（`calculateWeeklyMD`）按标准的 ISO 8601 周与实际工作日完美对齐，能精准计算人员在该自然周内的工时。
- **排期呈现重构与聚合折叠 (Allocation UI Aggregation)**：
  - 彻底解决了同一人员在同一项目由于多段碎排期而被拆散成数行堆叠的界面冗余。现在底层渲染时会按 `(人员, 项目)` 组合键强制聚合，跨越多周的碎片投入只用一行纵向平铺呈现。起止时间和投入占比会按宏观范围汇总展示。
- **项目级别的智能排期策略 (Project-Level Scheduling Strategy)**：
  - **全局到项目的策略下沉**：移除了大盘页面内的全局模式选择。现在排期策略（专注/均衡/紧急）直接下沉绑定到每个具体项目。
  - **基于规则的默认策略**：在 CSV 导入项目时，系统会自动评估项目特征。如果是高优先级（如 P0/P1 等）且开发人天 > 10 的项目，默认采用“均衡模式”；低优先级或开发人天 <= 10 的小项目则默认采用“专注模式”。
  - **按项目微调**：在“项目管理”列表中新增了“排期模式”下拉选项，允许项目经理针对每一个项目单独覆盖其排班策略。AI 调度引擎会在排期时逐个遵循各项目的模式。
- **运维工时按整日离散分配 (Whole-Day Ops Distribution)**：
  - 重写产品运维基石排期（PASS 0）的分配方式：将每月所需运维人天换算为若干个「整工作日」（按资源当日产能占用，满勤即 100%），并在当月可用工作日内均匀离散铺开，既避免运维扎堆在某个月的前几周，也修复了原「按整月低百分比摊薄（如每天约 5%）」方案下，周级看板逐周四舍五入归零、运维人员被排上却整行显示为空、一天都看不到的可视化缺陷。例如「每月 1 人天」的运维需求现在会落成 1 个满勤工作日，在对应自然周清晰显示为 `1`。

### 核心引擎与架构 (Core Engine & Architecture)
- **硬性“单周单项目”独占分配 (Single Project Per Week Constraint)**：
  - **引擎底层级拦截**：在生成候选人的排期日历槽（`getResourceCalendar`）阶段，增加了周维度的跨天穿透检查。任何员工（运维以外）一旦在某一周（比如 W20）被分配了一个核心业务需求，他在该周即便还有充足的人天产能，对其它所有平行需求也将被强制置为「0 产能封锁」。
  - **LLM 意图控制**：同步强化了大语言模型 (LLM) 的 AI Prompt 约束指令，明确声明：`A resource can ONLY be assigned to ONE project per week. Do not fragment their time across multiple projects in the same week.` 提升其给出的建议的一次性命中率与合法性。
- **排期策略全面可配 (Custom Strategy Prompts)**：
  - **自定义策略定义**：在系统设置页面新增三个独立的排期策略（单人模式、均衡模式、进阶模式）配置项，用户可以直接使用英文或中文编写针对大模型的指令逻辑。
  - **原生 Prompt 模板内嵌**：AI 主排期模板通过占位符自动组合以上三种策略指令，配合各项目的“排期模式”字段，指导大模型以最高效的方式调度不同特征的项目。
- **Jira 登录态智能检测 (Jira Auth Detection)**：
  - **精准认证拦截**：修复了未登录 Jira 时 API 请求被静默吞掉导致同步工时始终为 0 的问题。系统现在能够精准检测 `401 Unauthorized` 和 `403 Forbidden` 等认证阻断，以及 Jira Web 端的 HTML 登录重定向页面，并在界面上通过 ErrorModal 强制弹窗提醒用户先进行登录。
- **开源协议变更 (License Change)**：
  - 软件开源协议由 MIT License 更改为 **Apache 2.0 License**。


## [1.0.4] - 2026-05-25

### 新特性 (New Features)
- **Jira 管理模块 (Jira Management Module)**：
  - **一键同步 Epic 工时**：新增专门的「Jira 管理」侧边栏页面。系统能够自动识别项目中关联的 Jira Epic Key，支持通过一键点击批量调用 Jira REST API (JQL)，拉取对应 Epic 下所有子任务已记录的实际工时 (`timespent`)。
  - **智能模糊搜索 (Fuzzy Epic Matching)**：不再强制要求输入完整的 Jira Key。如果您在项目中填入了 Epic 的部分名称（如 `COMMON Security`），系统会在配置的 Jira 项目范围内进行智能前缀匹配（Starts With），自动寻找到对应的真实 Epic Key，然后精确拉取该 Epic 下的所有子任务工时。
  - **工时拉取鲁棒性增强**：针对部分 Jira Cloud 环境下 `timespent` 字段返回为空的问题，新增了对 `aggregatetimespent`（包含子任务的总计工时）和 `timetracking` 对象的深度扫描。系统现在能更全面地捕获已记录的工时，解决了同步后显示为 0 或只返回摘要的 Bug。
  - **动态 JQL 查询范围与 POST 查询引擎**：在「系统设置」中新增 Jira 关联项目配置。配置后，系统在拉取 Epic 工时时会在底层 JQL 自动拼接 `project in (...)` 条件，大幅提升查询速度并避免跨项目同名冲突。底层彻底迁移至 `POST /rest/api/3/search/jql`，完全解决了 GET 接口废弃报错以及长 JQL URL 超限问题。
  - **自定义工时折算率 (Custom Hours Per Man-Day)**：在系统设置中新增了工时折算参数。您可以根据团队的实际情况，自由配置“1 人天 (MD)”等于多少小时（默认 6 小时），系统会自动将拉取到的 `timespent` (秒) 按此比例折算。
  - **严格数据校验与智能路由**：底层 JQL 生成引擎增加智能路由逻辑。对于符合标准 Jira Key 格式的输入直接进入高精度匹配。
  - **工时分类统计与精确扣减 (Dev/Test Split Deduction)**：系统新增按 Issue Type 自动分类，将 Epic 下工时分别计入 `devLoggedMd` 与 `testLoggedMd`。支持在系统设置自定义测试类型（默认 Test/QA/Bug/Defect/测试/缺陷），并在排期时按开发/测试缺口精确扣减。
  - **同步体验增强 (Sync UX Upgrade)**：支持项目勾选同步（全选/反选）、30 分钟内重复同步提醒、上次同步时间展示与同步失败明细列表。
  - **双重计数防护与多关键词映射**：当 Issue 本身是 Epic 时仅使用 `timespent`，避免与子任务工时重复累加；同一 Epic 命中多个关键词时，工时会同步累加到所有匹配输入键。

### 修复与稳定性 (Fixes & Stability)
- **修复 Jira 搜索条件损坏 Bug**：修复了模糊搜索 JQL 丢失 `project in (...)`、`*` 通配符与 `created >= -365d` 过滤条件的问题，并恢复批量组合查询模式（所有 Epic Key 一次性传入），保证搜索可返回结果。

### 待开发/进行中 (Upcoming/In Progress)
- **排期引擎持续增强**：计划进一步优化资源匹配算法与异常场景下的自动容错逻辑。

## [1.0.3] - 2026-05-17

### 新特性 (New Features)
- **产品运维管理 (Product Operations Management)**：
  - **独立配置与管理模块**：新增了专门的产品运维配置页面，支持按产品名称配置每月的固定开发与测试运维人天（MD），并提供完整的增删改查及 CSV 批量导入/导出能力。
  - **技能字典联动生成**：无论是通过界面手动配置还是批量导入新的产品运维项，系统都会自动将其产品名称加入到全公司的「技能管理」字典中（标记为业务技能），极大地方便了后续对人员进行技能打标。
  - **PASS 0 按月基石排期算法**：排期引擎新增「阶段零」前置调度逻辑。在 AI 处理高优先级业务项目前，系统优先通过纯代码逻辑强制扣减运维人天。**系统采用精细的按月 (Month-by-month) 独立遍历机制，强制分配落在当月物理边界内**，彻底消除了多月运维人天在首月扎堆堆积的问题。
  - **智能好资源保护策略**：排期算法能够智能识别当前批次所有待排项目的“技术负责”与“测试负责”，在分配日常运维任务时，会自动避开这些核心 Leader（好资源），优先分派给仅拥有该产品技能标签的“基本资源”，实现人才效能最大化。
  - **大盘可视化标识**：在全局排期大盘中，自动为运维任务生成的虚拟分配记录（以绿色 `[运维] Product Name` 标识），使运维占用与常规项目一目了然。

### 修复与稳定性 (Fixes & Stability)
- **数据持久化重磅修复 (Critical Persistence Fix)**：
  - **修正 Dexie Schema 版本演进逻辑**：修复了在数据库版本升级过程中，因后续版本未完全定义所有数据表而导致部分表（如 `projects`, `resources`, `settings`）被意外删除的严重 Bug。现在所有版本定义均显式包含全量表结构，确保数据在 schema 变更时完美继承。
  - **稳定扩展 ID (Stable Extension ID)**：在 `manifest.json` 中配置了固定的 `key`（基于项目私钥提取）。解决了在开发环境下因 ID 随机变化导致 `chrome.storage.local` 和 IndexedDB 存储原点（Origin）重置的问题，保障了系统设置与业务数据的长期持久化。
- **统一报错反馈机制 (Unified Error Feedback)**：
  - **抽象化通用报错组件 (ErrorModal)**：封装了标准化的全局报错弹出窗口组件，替代了原始的 `window.alert()`。
  - **全量 UI 覆盖**：在「项目管理」、「人员管理」及「技能管理」等所有数据导入入口集成了该组件。当文件格式非法或解析失败时，系统将以更加专业、友好的图层模态框展示错误详情。
  - **日志追踪增强**：所有业务报错同步通过 `console.error()` 输出至浏览器控制台，并包含详细的技术上下文，极大提升了排障效率。
- **导入鲁棒性大幅提升 (Import Robustness)**：
  - **修复 `undefined` 报错**：修复了在导入项目、人员或技能时，因部分行数据缺失导致 `.includes()` 或 `.toString()` 调用崩溃的 Bug。
  - **修复中文乱码**：通过显式指定 UTF-8 编码提示，修复了部分不带 BOM 的 CSV 文件在导入时出现中文乱码（如 `çº¿ä¸`）的问题。
  - **支持忽略多余字段**：优化了列匹配逻辑，系统现在能更智能地识别核心表头并自动忽略文件中的无关冗余字段，增强了对不同格式 CSV/Excel 的兼容性。
### 优化与增强 (Improvements & Enhancements)
- **大盘可视化与交互升级 (Dashboard UI Optimization)**：
  - **动态折叠交互 (Collapsible Sections)**：全局排期大盘中的 5 个核心数据区块（已排项目、已排期任务详情、待跟进项目、待补充任务、待评估项目）全面升级为可折叠组件。用户可点击表头上的下拉箭头自由折叠或展开对应的表格，大幅缩短了页面长度，让项目经理在复盘复杂排期结果时能更聚焦于核心数据，免去频繁滚动页面的烦恼。
- **调度引擎核心性能优化 (Scheduling Engine Optimization)**：
  - **增量矩阵更新 (Incremental Matrix Update)**：重构了资源日历槽位矩阵的管理方式，引入内存共享的 `DailySlot` Map。在应用 AI 建议时仅对受影响人员执行增量更新，大幅减少了在 `applySuggestions` 内循环中触发全量审计 (`runAudit`) 的性能开销。
  - **O(1) 查找节假日 (O(1) Holiday Lookup)**：将 `dateUtils.ts` 中的节假日和调休日数组转换为 `Set<string>` 结构。在遍历海量日期范围时，将工作日判断的复杂度从线性查找降为常数级查找。
  - **矩阵共享与单次构建 (Single Calendar Build)**：确保 `runAudit`、日期推算及 AI 窗口生成共享同一套内存矩阵。排期过程中不再对同一个资源重复生成独立的日历副本，进一步降低了内存抖动和计算冗余。
  - **工作日预计算 (Working Day Pre-calculation)**：系统在排期开始时一次性构建排期窗口内的完整工作日 `Set`，后续所有 `getWorkingDays` 和 `calculateEndDate` 逻辑直接查表计数，彻底消除了重复的逐日逻辑判定。
- **排期准确性与健壮性提升 (Accuracy & Robustness)**：
  - **测试准入逻辑修正 (Test Entry Midpoint Logic)**：根据 PRD 规范，将测试人员的最早介入日期修正为该项目所有已分配开发任务时间跨度的**中点 (Midpoint)**，而非简单的开发开始日。这有效解决了测试资源在开发前期过早占位、导致空等的问题。
  - **回滚条件策略放宽 (Relaxed Rollback Conditions)**：增强了 PASS 2 的完整性审计逻辑。新增“开发严重欠配（已排 < 50%）且测试完全未排”的识别规则。此类项目会被果断回滚并加入重试队列，将宝贵的人力释放给后续能闭环的项目。
  - **计算精度全链路保持 (Full Precision Calculation)**：内部排期缺口 (`Gap`)、闲置天数 (`idleMd`) 及人天计算在流转过程中完全保留浮点精度，仅在写入 IndexedDB 持久化和 UI 最终渲染时执行四舍五入。有效消除了因连续取整导致的累积误差和边界值漂移。
  - **AI 结果严格校验 (Strict AI Response Validation)**：在解析 LLM 返回的 JSON 数组时，新增了严格的 Schema 过滤层。自动剔除 `projectId`/`resourceId` 缺失、分配人天小于 1 或百分比超出 (1-200%) 范围的脏数据，并记录警告日志。
  - **即时中断网络请求 (Immediate Fetch Interruption)**：在 `SchedulingContext` 中引入了 `AbortController`。现在点击「停止排期」不仅会停止逻辑循环，还会立即强制切断正在进行中的 AI HTTP 请求，确保网络资源即时释放。
- **支持「继续排期」功能 (Continue Scheduling Support)**：在全局排期大盘新增「继续排期」按钮。与「一键排期」清空重排不同，「继续排期」会保留 IndexedDB 中已有的分配数据，并在当前资源矩阵基础上增量排期，支持对新导入的项目进行追加分配或分阶段调度。
- **Token 消耗优化 (Cost Optimization)**：
  - **上下文上下文裁剪 (Context Trimming)**：对发送给 AI 的资源 `scheduleSummary` 字段执行智能裁剪。当空闲窗口信息过多导致 JSON 长度超限时，自动仅保留最近 3 个空闲区间，大幅缩减了高并发调度时的 Token 费用。
- **其他更新**：
  - **开源协议变更**：将项目的授权协议变更为 **MIT License**，鼓励社区贡献与二次开发，同时允许个人及商业用途的免费使用。

## [1.0.2] - 2026-05-03

### 优化与修复 (Improvements & Fixes)
- **持久化后台排期与手动中断**：
  - **切页不中断**：重构了调度引擎架构，将其提升至全局上下文。现在启动排期后，用户即使切换到其他管理页面，排期任务也会在后台持续进行，切换回大盘可无缝同步进度。
  - **停止排期功能**：新增了「停止排期」按钮，支持毫秒级的激进中断。点击后会立即抛出手动停止异常并切断后续 AI 调用，快速释放资源。
- **负责人强制锁定与明细匹配**：
  - 强化了 AI 调度指令，强制要求将项目定义的「技术负责人」与「质量负责人」优先排入对应项目，并确保其占有主要的分配比例，避免边缘化。
  - **业务明细联动**：AI 现在会深度解析任务明细中的产品子项（如 Order, POS, Stock 等），并自动关联人员的「业务领域能力」标签进行精准派发。
- **反碎片化排期算法优化**：在 AI 调度指令中新增了「最小分配单元」约束（强制 3 天起排，除非缺口不足）和「负责人集中化」规则。有效防止了在均衡模式下项目被过度拆解为多个 1-2 天极小碎片的现象，确保项目交付的连续性。
- **人员列表导出**：在人员管理页面新增了「人员导出」功能，支持将当前库中的所有团队成员及其角色、负荷、技能标签一键下载为 CSV 文件，方便备份与跨环境同步。
- **角色与技能联动优化**：精简了人员角色（移除了 APP 工程师），并实现了核心技能与「技能管理」页面的标签联动，支持通过点击标签快速指派能力。
- **技能标签管理页面**：新增独立的技能管理入口，支持对「业务领域能力」（更新默认值）与「技术能力」（更新默认值）进行标准化的 CRUD 管理。支持技能标签的批量导入、导出及模板下载功能。
- **人员管理批量导入**：在人员管理页面新增了上传 CSV/Excel 功能及下载导入模板功能。支持通过表头模糊匹配快速批量录入团队成员角色、负荷与技能标签。
- **已排项目概览看板**：在大盘详情上方新增「已排项目」区块，清晰列出已完成开发/测试双向资源匹配的项目，并展示技术负责人、质量负责人及投入人员明细。
- **默认策略调整**：排期大盘默认选择「均衡模式 (Balanced)」，鼓励分时并行以提升资源利用率。
- **调试能力增强**：在浏览器控制台新增详细的 [AI Debug] 和 [Hard Logic] 日志，完整追踪从 AI 建议到 JS 硬扣减的决策全路径。
- **版本迭代**：正式开启 1.0.2 版本的开发与演进。

## [1.0.1] - 2026-05-02

### 新特性 (New Features)
- **像素级时间槽位矩阵统筹 (Time Slot Matrix Scheduling)**：底层重构为基于「每日可用百分比」的日历矩阵。系统现在能感知资源在日历上的像素级分布，真正实现了 50%+50% 的多项目完美并行。
- **AI 窗口化感知与贪心收敛循环 (Convergence Loops)**：传给 AI 精确的 `Available Windows` 阵列，并引入最多 3 轮的收割循环。系统会持续尝试分配直到资源彻底收敛，资源利用率提升至 90% 以上。
- **回滚重试队列机制 (Integrity Retry Queue)**：被审计回滚的项目会自动进入重试队列，在全局收割阶段获得最高优先级，确保高优项目的残余缺口能被优先补齐。
- **AI 优先级微批次排期与完整性回滚 (Priority Mini-Batches & Integrity Rollback)**：
  - 弃用容易产生“先到先得”次优解的一把梭分配。现在系统严格按照 CSV 导入的高低优先级，将项目划分为“小批次”发送给大模型处理。
  - **排期完整性审计 (Integrity Audit)**：在所有批次排期完成后，系统会执行事务级检查，自动剥离并回滚只排上开发没排上测试的“半拉子工程”，将闲置资源退回。
- **AI 批量全局排期与 Prompt Caching (Global Batch Scheduling)**：通过合并单次调用的上下文并依托大模型的 Prompt Caching 机制，为批量分配节省了 90% 以上的 API Token 消耗。
- **多阶段迭代排期架构 (Two-Phase Scheduling)**：将排期阶段分离为 Dev-first 和 Test-second，优先安排开发任务。
- **测试前置依赖双重约束**：动态计算项目所有开发任务的起止时间中点作为测试最早开始时间，同时在 AI Prompt 中加入强制校验规则（确保测试不可早于开发），防止测试人员被盲目锁定。
- **AI 排期准则完全可配置 (Editable AI Prompt)**：在“系统设置”页面提供了一个可编辑的多行文本框，暴露了 AI 排期引擎的核心调度 Prompt。允许高级用户直接向大模型追加自定义的排期准则与限制，并提供了一键恢复系统默认规则的按钮。
- **排期策略动态切换 (Strategy Templates)**：新增“专注模式(100%投入)”、“均衡模式(50%分时并行)”与“紧急模式(超负荷推进)”等三种排期策略。
- **节假日与日历可配置 (Configurable Holiday Calendar)**：新增系统“节假日管理”页面，支持自定义调整全局法定假日和调休工作日，避免跨年问题。
- **严格排期时间窗口与 Token 优化 (Strict Window & Early Exit)**：
  - 系统强制以用户选择的结束月份为绝对物理边界（如选 4-6月则 6月30日为死线），超过此边界的项目需求将被截断为未排缺口。
  - **引擎熔断优化**：若某位工程师档期已被排满或越界，AI 引擎会自动停止在后续项目中询问该工程师；当所有工程师资源均被分配完毕时，系统将直接触发早期退出（Early Exit）中止后续循环请求，大幅节省 API Token 和执行时间。
- **一键下载导入模板 (Sample CSV Download)**：在项目管理页面提供带有标准多维表头及脱敏示例数据的 CSV 模板文件下载。
- **排期进度与错误反馈增强**：
  - 增加分布状态面板，实时反馈「初稿->审计->补排」的流转状态。
  - 优化错误弹窗提示（AI Key 过期、网络报错等）及一键跳转配置功能。

### 优化与修复 (Improvements & Fixes)
- **杜绝“0天排期”问题**：底层强依赖代码进行 `Math.min` 硬扣减截断计算，并增设严格的 $\ge$ 1 整数人天过滤逻辑。
- **重构全栈工程师职责**：将“全栈工程师”的默认匹配严格限定在开发（Dev）阶段（禁止其代替测试），以保证核心测试人员利用率。
- **增强文件导入兼容性 (Enhanced File Import)**：改用智能表头模糊匹配替代按列号读取，支持识别 `Tech Lead`、`Quality Lead`、`Details Product MD`、`Tech Stack` 及 `Domain` 等多个扩展业务字段，并在数据大盘中结构化展示。
- **项目过滤与分离展示**：大盘中自动隔离 `devTotalMd` 和 `testTotalMd` 均为 0 的项目，放置在底部“待评估项目”清单中，不参与 AI 排期。
- **已排期任务分组视图**：大盘支持「按人员分组」与「按项目分组」双向切换，方便多维统计查看。
- **人员管理体验优化**：修复人员负荷滑动条（Range Input）因步长限制无法精确设置为 0% 或 100% 的边界值问题。
- **时间合法性容错校验**：增强了日期有效性校验，修复了因导入脏数据导致的 `RangeError: Invalid time value`。
- **精简 Chrome 权限**：移除了未使用的 `alarms` 权限，以满足 Chrome Web Store 审核的安全规范。
- **完善 Chrome Web Store 上架素材**：生成了全新的 SVG & PNG (`16/48/128px`) 品牌 Logo 并引入自动化打包与发布命令 (`npm run publish`)。

## [1.0.0] - 2026-04-26

### 初版发布 (Initial Release)
- **核心排期引擎上线**：发布基于纯客户端 (Local-first) 架构的 Intelligent Resource Planner (IRP) 首个正式版本，所有数据缓存在浏览器本地 IndexedDB，彻底保障数据隐私。
- **项目数据接入**：
  - 支持通过上传 CSV 或 Excel (.xlsx) 文件，一键批量导入待排期的项目清单、优先级和预估的研发/测试工时。
  - 项目优先级严格遵循导入文件从上到下的物理顺序，确保符合现实管理直觉。
- **人员与资源管理**：
  - 提供可视化的人员管理面板，支持录入团队成员的专业角色（前端/后端/全栈/APP/测试）、当前可用负荷比例（1-100%）及各类技术栈技能标签。
- **AI 智能排期大盘 (Dashboard)**：
  - 接入大语言模型 (支持 OpenAI 及各类兼容的本地/云端 LLM 接口)，基于全局待排项目和可用资源池一键生成智能排班建议。
  - 独创的双向核算审计看板：直观展示每个项目的剩余人天需求（资源缺口），以及每个团队成员的当前闲置时长（空置资源）。
  - 根据分配占比和项目周期，自动推算跨月份的有效投入人天（MD）。
- **Jira 深度集成与原生级预警 (Content Script)**：
  - 插件支持在后台异步关联 Jira 平台的工作动态。
  - 当项目经理浏览 Jira 的 Issue 页面时，系统会实时读取该 Issue 负责人的本地排期库负荷，并在页面 DOM 内无侵入地悬浮注入其“红黄绿”过载状态，提供风险干预预警。
- **系统底层与合规**：
  - 完整的选项配置页面，支持自定义配置大模型 API 域名、密钥与指定模型。敏感密钥使用 Chrome Storage 隔离加密存储。
  - 标准化开发文档、打包流程、隐私合规声明 (Privacy Policy) 以及 SVG 品牌规范已初步建立。
